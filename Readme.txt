#If you are running from original dataset , drag and drop the dataset_seperation.py into the folder at the same level as folders "a ,b,c,d...."
#Run CNN.py to train and test the classifier
#Ensure your current working directory is set to the CNN.py file location
